#include <bits/stdc++.h>

using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < (n); ++i)

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string S;
    cin >> S;
    string res;
    rep(i, S.size()) {
        if (i == S.size() / 2) continue;
        res += S
    }
    string res =
}
